# Modified from ChatGPT response asking how to have hot reload docker container for .net 

FROM mcr.microsoft.com/dotnet/sdk:9.0

WORKDIR /app

COPY OurCity.Api/*.csproj .
RUN dotnet restore

#non-root user
USER app

EXPOSE 8000
CMD ["dotnet", "watch", "run", "--urls", "http://0.0.0.0:8000"]
